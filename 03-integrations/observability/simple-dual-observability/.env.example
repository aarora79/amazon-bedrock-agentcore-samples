# Simple Dual Observability - Environment Configuration Template
# This file is committed to the repository as an example.
# Copy this to .env and fill in your actual values.
# DO NOT commit .env to git (it's in .gitignore)

# AWS Configuration
AWS_REGION=us-east-1

# AWS Profile (Optional)
# Use if you have multiple AWS credential profiles configured locally
# If not set, AWS CLI will use the default profile
# Example: AWS_PROFILE=dev-profile
# See: https://docs.aws.amazon.com/cli/latest/userguide/cli-configure-profiles.html
AWS_PROFILE=default

# Braintrust Configuration (Optional)
# Get your API key from https://www.braintrust.dev/app/settings/api-keys
# Get your Project ID from your project URL (https://www.braintrust.dev/app/ORG/p/PROJECT_ID)
# Leave these empty or omit them to use CloudWatch observability only
BRAINTRUST_API_KEY=your-api-key-here
BRAINTRUST_PROJECT_ID=your-project-id-here

# AgentCore Configuration (Needed only for observability demo)
# Get this from deploy_agent.sh output after deployment
# Or retrieve from AWS Console: AgentCore Runtime > Agents
AGENTCORE_AGENT_ID=your-agent-id-here

# Gateway Configuration (Optional - for Lambda-based tools)
# Set USE_GATEWAY=true to deploy tools as Lambda functions via AgentCore Gateway
# Set USE_GATEWAY=false (or omit) to use local tools within agent container
USE_GATEWAY=false

# Gateway ARN (Required only if USE_GATEWAY=true)
# Get this from configure_gateway.py output after gateway configuration
# Or retrieve from AWS Console: Bedrock > AgentCore > Gateways
# Example: GATEWAY_ARN=arn:aws:bedrock:us-east-1:123456789012:gateway/abc123
GATEWAY_ARN=your-gateway-arn-here

# Cognito Configuration (Required only if USE_GATEWAY=true)
# These credentials are needed for Gateway authentication
# Get these from setup_cognito.py output after Cognito setup
# COGNITO_DOMAIN should include the protocol (https://)
COGNITO_DOMAIN=https://your-cognito-domain.auth.us-east-1.amazoncognito.com
COGNITO_CLIENT_ID=your-client-id-here
COGNITO_CLIENT_SECRET=your-client-secret-here
